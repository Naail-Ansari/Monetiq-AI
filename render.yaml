services:
  - type: web
    name: monetiq-ai
    env: docker
    plan: free
    region: oregon
    branch: main
    repo: https://github.com/YOUR_USERNAME/YOUR_REPO_NAME
    dockerContext: .
    dockerfilePath: Dockerfile
    healthCheckPath: /
    autoDeploy: true
    envVars:
      - key: PORT
        value: 10000
